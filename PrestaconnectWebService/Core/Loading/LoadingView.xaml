<Window x:Class="PrestaconnectWebService.Core.Loading.LoadingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrestaconnectWebService.Core.Loading"
        mc:Ignorable="d"
        Height="100" Width="250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border BorderThickness="2" CornerRadius="6" Grid.RowSpan="2"
          BorderBrush="{DynamicResource ResourceKey=SelectedBackgroundBrush}"
          Background="{DynamicResource ResourceKey=WindowBackgroundBrush}"/>

        <Label FontSize="24" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Content="Chargement en cours"></Label>
        <Grid Grid.Row="1" Visibility="Collapsed"></Grid>
    </Grid>
</Window>
